# Core
Django>=5.1,<5.3
djangorestframework>=3.15
django-environ>=0.11
psycopg[binary]>=3.2

# Async / real-time (Channels wired up in Stage 4, ASGI entrypoint from Stage 0)
channels[daphne]>=4.1
channels-redis>=4.2
uvicorn[standard]>=0.30

# Background jobs
celery[redis]>=5.4
django-celery-beat>=2.7
redis>=5.0

# Auth
PyJWT[crypto]>=2.9
certifi>=2024.0
svix>=1.30

# API schema
drf-spectacular>=0.27

# Testing
pytest>=8.0
pytest-django>=4.8
pytest-asyncio>=0.24
pytest-mock>=3.14

# Maps (Distance Matrix ETA)
requests>=2.32
